class Controller:
    def __init(x0,y0,theta0,fuel,t_step)__:
        """
        Takes in the initial conditions and saves them
        """
        self.x = x0
        self.y = y0
        self.theta = theta0
        self.fuel = fuel
        self.dt = t_step

    def optimal_path(xf, yf):
        """
        Finds the optimal path for landing at a particular spot on the map
        """

    def step(t):
        """
        Returns four values: left, right, rotate_left, rotate_right
        These are the controls that dictate how the craft moves
        """

    def update(x

    
